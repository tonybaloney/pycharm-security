name: 'Testing Custom Action'
on: [push]

jobs:
  test_example:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Run PyCharm Security
        id: pycharm-security
        uses: tonybaloney/pycharm-security
        with:
          path: $GITHUB_WORKSPACE